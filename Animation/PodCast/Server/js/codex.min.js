
jQuery(document).ready(function(){
  var ctx = $("#my_canvas")[0].getContext('2d');
  var al = 0;
  var start = 4.72;
  cw = ctx.canvas.width;
  ch = ctx.canvas.width;
  var diff;
  function progressBar(){
    diff = ((al / 100) * Math.PI * 2 * 10).toFixed(2);
    ctx.clearRect(0, 0, cw, ch);
    ctx.lineWidth = 17 ;
    ctx.font = "20px Arial";
    ctx.fillStyle = '#39aa32';
    ctx.strokeStyle = '#39aa32';
    ctx.textAlign = 'center';
    ctx.fillText(al+'%',cw*.5,ch*.5+2, cw);
    ctx.beginPath();
    ctx.arc(120, 120, 100, start, diff/10+start, false);
    ctx.stroke();

    if(al >= 100){
      clearTimeout(sim);
      //Added my redirecting code here
      window.location.href = "PodCast";
    }
    al++;
  }


  var sim = setInterval(progressBar, 50);
})






























  /***
Jquery function for animations
***

function collapseNavbar() {
  if ($(".navbar").offset().top > 50){
    $(".navbar-fixed-top").addClass("top-nav-collapse");
  }else{
    $(".navbar-fixed-top").removeClass("top-nav-collapse");
  }
}

$(window).scroll(collapseNavbar);
$(document).ready(collapseNavbar);

$(function() {
    $('a.page-scroll').bind('click', function(event) {
        var $anchor = $(this);
        $('html, body').stop().animate({
            scrollTop: $($anchor.attr('href')).offset().top
        }, 1500, 'easeInOutExpo');
        event.preventDefault();
    });
});

/*Jquery with animate.CSS
jQuery(document).ready(function() {
	jQuery('#dog').addClass("hidden").viewportChecker({
	    classToAdd: 'visible animated fadeIn', // Class to add to the elements when they are visible
	    offset: 100
	   });
}); *

function animate(){
  $(".doing").addClass("hidden").viewportChecker({
    classToAdd: 'animated fadeInUp lightSpeedIn',
    offset:100
  });
}

$(document).ready(animate);*/
